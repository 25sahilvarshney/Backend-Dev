//borrowing the books from the library
const fs = require("fs");
function borrowRecord(memberid, bookid){
    try{
        if(membership==bookid){
            return "book borrowed successfully";
        }
    }
    catch(error){
        console.log("error");
    }
}